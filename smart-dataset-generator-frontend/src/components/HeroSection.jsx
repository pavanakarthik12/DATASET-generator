import React from 'react';

const HeroSection = () => {
  return (
    <section className="hero">
      <h1>Welcome to Dataset Generator</h1>
      <p>
        Generate comprehensive datasets from multiple sources including weather, 
        stocks, news, images, and COVID-19 data. Export in CSV, JSON, and Parquet formats.
      </p>
    </section>
  );
};

export default HeroSection;
